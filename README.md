# Science Hub — Master Binder

Small static site used as a teacher-facing "master binder" and curriculum hub. No build system: plain HTML/CSS/JS with Tailwind and Google Fonts loaded from CDN.

## Quick start

- Open `index.html` in your browser for the dashboard.
- Or serve locally for proper routing/assets:
```bash
python3 -m http.server 8000
# open http://localhost:8000